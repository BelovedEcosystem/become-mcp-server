# BECOME MCP Server — Repository & Design Audit (2026-09-10)

> **Historical context note (added 2026-09-16):** This audit was conducted when BECOME was settling certified jobs on Sepolia testnet. As of 2026-09-01, **production BECOME settles on Ethereum mainnet** via the PayHook contract. Sepolia references in this document reflect the system state at audit time (2026-09-10) and are retained for historical accuracy.

Scope: everything in this repository (`README.md`, `docs/CLAUDE-CONNECTOR.md`,
`skills/become-use/SKILL.md`, `.gitignore`, `LICENSE`, full git history) plus the
publicly documented design of the hosted service. The live host
(`mcp.belovedecosystem.com`) was **not reachable** from the audit environment
(egress proxy 403), so runtime behaviour (TLS, 401 handling, `/agent.md`
content, tool schemas) is **unverified**.

Severity scale: **High** = fix before wider distribution · **Medium** = fix soon ·
**Low** = hygiene / clarity.

---

## 0. Headline

The repository contains **no server source code**. It is documentation plus an
agent skill. Nothing about auth enforcement, input validation, job isolation,
rate limiting, or logging can be audited from here. Every finding below is
therefore about (a) what the docs promise and (b) what the docs leave unsaid.

---

## 1. Security findings

### H-1. Single shared static API key, no scoping, rotation, or revocation story
`README.md` §Auth: one `BECOME_MCP_API_KEY`, minted out of band, accepted as
`Authorization: Bearer` **or** `X-Api-Key`.

- No per-client / per-scope keys are described despite the README saying "ask
  for a *scoped* key". Document what a scope actually restricts.
- No rotation or revocation procedure. A leaked key (e.g. a pasted chat log,
  a screenshot of a connector settings page) has unbounded lifetime.
- Accepting the key in two different headers doubles the places it can leak.
  `Authorization` is routinely redacted by proxies, CDNs and log pipelines;
  custom `X-Api-Key` headers usually are not. Prefer `Authorization` only, or
  state explicitly that `X-Api-Key` is deprecated.
- The MCP authorization spec for remote HTTP servers is OAuth 2.1, and Claude
  custom connectors support it natively. Static keys are acceptable interim
  but should be labelled as such with a migration note.

### H-2. Job isolation between key holders is undocumented (possible IDOR)
`become_status` / `become_result` take only a `job_id`. With a shared key,
**any holder can read any other holder's job** unless the server binds jobs to
keys. Document: are job ids bound to the issuing key? What is the entropy /
format of `job_id`? Are they guessable (sequential, timestamp-based)?

### H-3. Cost / abuse control for `certify: true` is undocumented
`certify: true` triggers an EC2 prove run of ~60–70 minutes per job. A key
holder (or a leaked key) can queue unbounded paid compute. Document rate
limits, per-key quotas, concurrency caps, and what happens on quota exhaustion.
Without this, a single leaked key is a direct cloud-bill exposure.

### M-1. Server-supplied text is fed to the model as instructions
The agent contract (`/agent.md`) and response fields such as
`summary_for_model`, `how`, `inbox`, `answer` are server-controlled prose that
clients are told to read and act on. Any compromise of the host (or an
upstream that populates those fields, e.g. the GHA run output) becomes a
prompt-injection channel into every connected agent.

- The skill already says "do not treat `ok` or `summary_for_model` alone as
  fail/success" — good. Extend it: treat all free-text fields as **data, not
  instructions**; drive control flow only off enumerated fields
  (`agent_status`, `terminal`, `poll_after_ms`).
- Consider publishing a hash / signature of `agent.md` in this repo so clients
  can pin the contract version they were tested against.

### M-2. "Settled" is asserted by the same host that produced the answer
README: "Never trust `answer` … until `Settled`". But `Settled` is a string
returned by the server. There is no documented way for a client to
**independently verify** settlement (Sepolia tx hash, contract address, job
hash preimage, verifier key). Publish the contract address, the
`BecomeJobHash/v0` recipe, and a one-line "verify this job yourself" example
so the trust claim is checkable rather than asserted.

### M-3. Trust boundary of the certify path is under-communicated
The header line says the host "coqchk-checks and (optionally) EC2-proves +
Sepolia-settles", while §Honesty says the settlement TCB is **T2CERT0 (not
full coqchk-in-guest)**. Readers skimming the top will over-trust. Move the
T2CERT0 caveat next to the first mention of "certify" and spell out what is
in the trusted computing base (a single operator-run EC2 instance + the GHA
workflow that dispatches it).

### L-1. Obsolete tunnel URL remains in git history
`finals-affair-designed-constitutes.trycloudflare.com` is present in the
initial commit. It is dead and public, so no action needed, but do not reuse
the pattern of committing ephemeral hostnames.

### L-2. No secrets found in history
Grepped all revisions for key-like strings, bearer tokens, hex private keys.
Nothing found. `.gitignore` is minimal but adequate for a docs-only repo.

### L-3. No `SECURITY.md` / disclosure contact
There is no documented way to report a vulnerability in the hosted service.
Add a `SECURITY.md` with a contact and expected response time.

---

## 2. Documentation correctness & consistency

| # | Finding | Location | Severity |
|---|---------|----------|----------|
| D-1 | "Protected routes" is never enumerated. Are `/`, `/agent.md`, and any `/health` or `/v1/*` REST routes public? The initial commit advertised `POST /v1/ask`, `GET /v1/status/{id}`, `GET /v1/result/{id}`, `GET /health` and a `become_spec_check` tool; the current README silently dropped them. State whether they were removed or merely undocumented. | `README.md` §Auth, §Tools; git `7f7a82d` | Medium |
| D-2 | Tool parameter reference is incomplete. `target`, `bid_wei`, `chain`, `certify`, `wait`, `due_date` are mentioned in prose; there is no schema (types, required/optional, defaults, allowed `chain` values, units for `due_date`). Response fields (`agent_status` enum, `terminal`, `poll_after_ms`, `how`, `run_url`, `actions_url`, `progress`, `proof_present`, `failed`, `in_progress`, `auto_generate`, `run_id`, `answer_v`, `visible_result`) are likewise never tabulated. Agents will guess. | `README.md`, `SKILL.md` §3–5 | Medium |
| D-3 | The `agent_status` state machine is implied but never drawn. List every value and the legal transitions (e.g. `Queued → Proving → Settled | Failed`). | `SKILL.md` §4–5 | Medium |
| D-4 | `mcp.json` example uses `${BECOME_MCP_API_KEY}` under a "Cursor / HTTP MCP" heading. Env interpolation syntax differs by client (Claude Code accepts `${VAR}`; Cursor documents `${env:VAR}`). Either show both or label the example by client. | `README.md`, `SKILL.md` §1 | Low |
| D-5 | Dangling cross-reference: `GROK-CONNECTOR-PACKAGING.md` in `become-dvm-interim`. No link, and that repo may be private. Link it or remove the reference. | `README.md` §Grok | Low |
| D-6 | `npx skills install BelovedEcosystem/become-mcp-server` — the `skills` CLI and its repo-slug install form are not verified here. Confirm the command works from a clean machine. | `README.md`, install section | Low |
| D-7 | Anthropic connector link is written with `remote-mcp` in README/SKILL and `remote-MCP` in `docs/CLAUDE-CONNECTOR.md`. Slugs are case-sensitive on some hosts; use one form. | `docs/CLAUDE-CONNECTOR.md` | Low |
| D-8 | No versioning of the contract. `/agent.md` can change under clients at any time. Add a `contract_version` (and surface it in tool responses) so the skill can detect drift. | all | Medium |
| D-9 | No CHANGELOG, no CI, no link checker, no CODEOWNERS. For a docs-only repo a link check + markdown lint workflow is cheap and would have caught D-5/D-7. | repo root | Low |

---

## 3. Agent skill (`skills/become-use/SKILL.md`) review

**Good:** clear happy path; explicit "do not invent job ids"; "poll only when
`auto_generate` / `run_id` present"; Settled-only trust rule; honesty footer.

**Improve:**

1. Add a **failure path**: what to do on `Failed`, on `gallina_required`, on a
   401, on `poll_after_ms` exceeding some ceiling, and a maximum poll count so an
   agent does not spin for hours on a stuck job.
2. Add a **Gallina input hygiene** rule: the target is compiled server-side.
   Tell agents to send only the proposition (no `Require`, no `Ltac`, no
   `Extraction`, no shell-adjacent Coq commands) and to keep it under a stated
   size limit. Whether the server sandboxes Coq is unknown (no source), so the
   skill should be conservative.
3. State that `run_url` / `actions_url` point at a GitHub Actions run; agents
   with GitHub tooling must **not** re-dispatch, cancel, or comment on it.
4. `disable-model-invocation: false` is fine, but add `allowed-tools` (or the
   equivalent for the skills CLI) listing only the four `become_*` tools, so a
   client cannot be talked into using unrelated tools by response text.

---

## 4. What could not be audited (needs source or host access)

- Auth middleware: constant-time key compare, which routes are gated, 401 vs
  403 semantics, whether keys are logged.
- Input validation on `target` (Coq/Gallina) and whether `coqchk` / `coqc`
  run in a sandbox with time and memory limits.
- Job store: id generation, key binding, retention, PII.
- Transport: TLS config, HSTS, CORS on `/mcp`, MCP session-id handling,
  Origin validation (required by the MCP spec for HTTP transports to prevent
  DNS-rebinding).
- Rate limiting and per-key quotas.
- The EC2 prove pipeline, S3 staging, and GHA dispatch credentials.

**Recommendation:** publish the server source (or grant read access to the
repo that holds it) and re-run this audit against code. Until then the public
badge "Auth: API key required" is a claim, not a verified property.

---

## 5. Prioritised action list

1. Document job↔key binding and `job_id` format (H-2).
2. Document per-key quotas / rate limits for `certify` (H-3).
3. Add key rotation + revocation procedure; deprecate `X-Api-Key` (H-1).
4. Publish independent settlement-verification recipe + contract address (M-2).
5. Add a full tool/response schema and `agent_status` state table (D-2, D-3).
6. Add `contract_version` and pin it in the skill (D-8).
7. Add `SECURITY.md` (L-3), fix dangling refs (D-5, D-7), add link-check CI (D-9).
8. Extend the skill with failure handling and input hygiene (§3).

---

## Addendum (same day): connection retry and branch `claude/become-mcp-setup-pkqgke`

### Live connection: still blocked, retried
Retried `GET /agent.md` and a JSON-RPC `initialize` POST to `/mcp`. Both were
refused by the session's egress proxy (`CONNECT` → 403, organisation policy),
before any packet reached the host. No BECOME connector is attached to the
session and no `BECOME_MCP_API_KEY` is present. Runtime findings remain
**unverified**. Unblocking requires either attaching the server as a Claude
connector, or allow-listing `mcp.belovedecosystem.com` and setting the key in
the environment, then starting a new session.

### The setup branch contradicts `main` on the public contract (High)
Branch `claude/become-mcp-setup-pkqgke` (commit `e5ae41b`, "point BECOME MCP at
stable host, Bearer auth, and become_orchestrate") rewrites the README and
skill and deletes `docs/CLAUDE-CONNECTOR.md`. Compared with `main` it:

| Topic | `main` | setup branch | Risk |
|---|---|---|---|
| Input shape | Gallina `target` only; NL-only → `gallina_required` | plain-language `question`; "agent should not paste Coq" | Agents following the branch will get `gallina_required` on every call if `main` is right, or vice-versa. Only one can match the server. |
| Honesty section | BecomeJobHash/v0, **T2CERT0** TCB caveat, Sepolia ≠ mainnet | removed; replaced by generic "TEST_ONLY" label | Drops the single most important trust disclosure. |
| Tools | 4 tools | 5 tools + 4 REST routes (`/v1/ask`, `/v1/status/{id}`, `/v1/result/{id}`, `/health`) | Re-advertises surface that `main` had removed. Unknown whether it exists. |
| Proving guidance | poll rules, `auto_generate`/`run_id` → poll only, `summary_for_model` warning | removed | Loses the anti-injection and anti-double-dispatch rules. |
| Key in config | `${BECOME_MCP_API_KEY}` env interpolation | literal placeholder `Bearer YOUR_BECOME_MCP_API_KEY` | Invites users to paste the real key into a config file that is then committed. Worse than `main`. |
| Status badge | "API key required" | "Interim TEST_ONLY" | Back to interim framing after `main` declared production. |
| Tagline | "AI clients ask Gallina questions" | "plain language in, Coq-checked answers out" | Same contradiction as row 1. |

The branch is essentially the *initial* commit's wording with the stable
hostname substituted. It looks like it was written from the old README rather
than from `main`. **Do not merge it as-is.** Decide which input contract the
server actually implements, then make `main`, the skill, and `/agent.md` agree.

### One thing the setup branch gets right
Its skill adds an explicit "What not to do" list (no key echo in tool
arguments, no claiming production settlement without evidence). Port those
bullets to `main`'s skill; discard the rest.

---

## Addendum 2: live `tools/list` from the production host (via Claude connector)

The user attached the server as a claude.ai connector. The session received
the real tool list before the connector's token expired, so the following is
**verified against production**, not the docs.

### Tools actually exposed (8, docs say 4)
`become_orchestrate`, `become_ask`, `become_status`, `become_result`,
`become_cancel`, `become_encode_service_request`, `become_engine_query`,
`become_spec_check`. The README on `main` lists four. `become_cancel` and
`become_encode_service_request` appear in no doc anywhere.

### Contract dispute settled: `main` is right, the setup branch is wrong
The live schema states: "Gallina Target is the ask. Natural-language request
is gloss only … Never invents Target when target is missing." An
English-only call cannot succeed. The `claude/become-mcp-setup-pkqgke` branch
tells agents the opposite and must not merge.

### Security findings from the live schema

| # | Finding | Sev |
|---|---|---|
| S-1 | **Client-controlled server-side URL.** `become_orchestrate.engine_url` and `become_engine_query.url` default to `http://127.0.0.1:8787` and accept any string. A caller can make the host fetch arbitrary internal or external URLs (SSRF: cloud metadata endpoint, other loopback services, the S3/GHA plumbing). Remove these parameters from the public schema; read the engine URL from server config only. | **High** |
| S-2 | **Client-controlled filesystem path.** `become_encode_service_request.job_dir` is a raw path "to job directory". Any key holder can point it at any directory on the host. Replace with `job_id` and resolve the path server-side under a fixed root. | **High** |
| S-3 | **Client-chosen job ids.** `become_orchestrate.job_id` is an "optional fixed job id". With a shared key, a caller can collide with or overwrite another tenant's job, and the IDOR from the first report becomes trivial. Ignore client ids; always mint server-side. | **High** |
| S-4 | **Operator knobs exposed to clients.** `unload_ollama`, `query_engine`, `timeout_s` guidance about "~16GiB host", `BECOME_ALLOW_LOCAL_GROTH16`, and `anvil` chain are host-operations concerns. They leak infrastructure detail (host RAM, local LLM, local prove flag) and let clients change host behaviour. Strip them from the public surface. | Medium |
| S-5 | **`oneOf` enforced by prose only.** `become_orchestrate` has `required: []`; "exactly one of target / question_v" lives in the description. A call with neither, or both, reaches the handler. Encode it in the JSON schema. | Low |

### Friction findings (why a chatbot fails on first contact)

1. The orchestrate description is ~120 words of internal jargon ("gloss",
   "SR fields", "T2CERT0", "claimant/pins"). A model that has not read
   `/agent.md` cannot tell what to send. Lead with one sentence and one
   example: *Send `target: "exists n : nat, 5 + 1 = n"`. Answer arrives as
   the numeral "6" when `agent_status` is `Settled`.*
2. Parameter count: orchestrate has 19 parameters. A chatbot needs 3
   (`target`, `certify`, `wait`). Hide the rest behind auth scopes or delete.
3. `a` / `b` "operand overrides only after Gallina target is present" is a
   trap: it invites the model to skip the target.
4. `become_ask` requires `bid_wei` but orchestrate defaults it; two tools,
   two rules. Merge into one tool.
5. No `outputSchema` on any tool, so `agent_status` values, `terminal`, and
   `poll_after_ms` are undiscoverable until the first response.
6. The description says "Prefer wait=false + poll_after_ms" but there is no
   guidance on how many polls or what a stuck job looks like.

### Connector-flow finding
The token issued by the connector expired within the session. Combined with
`initialize`/`tools/list` gating, a chatbot's first experience is a working
tool list followed by "requires re-authorization" on the first call. Long
token lifetimes for zero-bid scope, or an unauthenticated zero-bid tier, fix
this.

---

## Addendum 3: server source audited and fixed (`BelovedEcosystem/become-dvm-interim`)

The host code was pushed to a private repo. Findings confirmed in code and
fixed in two pull requests:

| Finding | Confirmed at | Fix |
|---|---|---|
| S-3 client-chosen `job_id` used as a directory name → traversal / collision | `mcp_server/easy.py` `easy_status`/`easy_result` (`JOBS / job_id`), `orchestrate.py` (`dest = JOBS / jid`) | `mcp_server/job_ids.py` validator; public orchestrate never accepts a client id (PR #1) |
| S-1 SSRF via `engine_url` / `become_engine_query.url` | `server.py` `call_tool` passthrough; `orchestrate._query_engine` | Removed from schema and passthrough; URL only from `BECOME_ENGINE_URL`; loopback/configured-host allowlist (PR #1) |
| S-2 raw `job_dir` path | `become_encode_service_request` | Takes `job_id`, resolved under jobs root (PR #1) |
| S-4 operator knobs (`unload_ollama`, `query_engine`) | schema + passthrough | Removed; env-driven (PR #1) |
| **New:** six unadvertised `job_dir` tools reachable via `tools/call` (incl. `become_ask_nat_toy` writing with `force=True`) | `server.call_tool` dispatch | Names outside `TOOLS` refused; local-dev opt-in `BECOME_MCP_LOCAL_TOOLS=1` (PR #1) |
| **New:** no Origin validation on Streamable HTTP | `http_api.py` | `_require_origin` on `/mcp` and `/message` (PR #1) |
| Friction: nothing callable without a secret | `http_api.py` POST `/mcp` gating | Flag-gated anonymous zero-bid tier, session-bound jobs, readable `auth_required` (PR #2) |

Pull requests: `become-dvm-interim` #1 (hardening) and #2 (anonymous tier,
stacked on #1). Baseline test suite unchanged (33 environmental failures in
the sandbox, all Coq/fixture/JWT-stub related).

Still open on the server side: OAuth Wave-2 apply (Cognito) per the repo's
own design; `outputSchema` on `become_status`/`become_result`; the long tool
descriptions.

---

## Addendum 4: live customer test PASSED (anonymous tier on production)

Operator merged `become-dvm-interim` #1–#4 and set `BECOME_MCP_ANON_ZERO_BID=1`.
Tested from a Claude connector with **no key and no OAuth token**:

| Step | Result |
|---|---|
| `become_orchestrate` `target="exists n : nat, 5 + 1 = n"`, `bid_wei=0`, `certify=false`, `wait=true` | `agent_status=Settled`, `answer="6"`, `job_id=orch-5plus1-3af055ee`, `job_hash=0x37cd…c644`, single call |
| `become_result` on my own job | full result returned, including `answer.v` |
| `become_status` on the operator's job `orch-5plus1-c5c47335` | denied: `code=auth_required`, readable `how` pointing at the OAuth PRM (session binding works) |
| `become_status` with `job_id="../../etc"` | denied: `code=auth_required` (never reaches the filesystem) |
| `certify=true` | not exercised: the test client's own policy blocked a paid-compute call; the operator reports certify/paid remain gated |

The two-minute acceptance test in `docs/LEAST-FRICTION.md` is met: URL in,
Settled answer out, zero secrets.

Note for the connector: the tool list cached by the client still showed the
pre-hardening schema (`engine_url`, `job_id`, `job_dir`). The server ignores
those fields now; clients pick up the new schema on reconnect.

---

## Team note: what "Verified" means on the anonymous tier (2026-09-10)

**Request:** `45 + 5`, zero bid, "Verified".

**What happened, in order:**

1. Called `become_orchestrate` with `target="exists n : nat, 45 + 5 = n"`,
   `bid_wei=0`, `certify=true`, `chain=sepolia`, through an **anonymous**
   Claude connector (no API key, no OAuth token).
2. Server refused before doing any work. Tool result, not a transport 401:
   `code=auth_required`, `how="certify=true or bid_wei>0 needs an API key
   or OAuth login (scope ask:bid). Anonymous tier: certify=false, bid_wei=0
   only. Get access: /.well-known/oauth-protected-resource"`.
3. Re-ran with `certify=false`. Settled in one call:
   `job_id=orch-45plus5-5ae6e68b`, `answer="50"`,
   `job_hash=0x862045488aa168b0f00ce929341e61727d0724c84bee6334eb192976af295871`,
   `certified=false`, `receipt_url=null`.

**Two different guarantees:**

| | Free tier (`certify=false`) | Certified (`certify=true`) |
|---|---|---|
| Who may call | Anyone with the URL | Key or OAuth `ask:bid` scope |
| What runs | `coqc` + `coqchk` on the MCP host | Groth16 proof on EC2 (~60 min), then Sepolia settlement |
| What you get | Settled numeral + `job_hash`; `answer.v` on request | Same, plus `fill_tx` / `receipt_url` on Sepolia |
| Trust boundary | The MCP host's Coq checker | T2CERT0: guest certificate verify, **not** full coqchk-in-guest; Sepolia ≠ mainnet |
| Cost to operator | Seconds of CPU | EC2 spend + gas |

**Why the refusal is correct:** the anonymous tier exists so a first-contact
chatbot can get a real checked answer with zero secrets. Certified runs cost
money and produce on-chain artifacts, so they stay behind identity. The gate
returning a readable tool result (instead of a 401) is what lets a model
explain the situation to its user rather than fail silently.

**To get a certified `45 + 5`:** connect with an operator key or OAuth, then
call with `certify=true`, `wait=false`, and poll `become_status` until
`Settled`; expect `fill_tx` and `receipt_url`. Do not describe a free-tier
result as chain-settled.

---

## Addendum 5: owner decisions applied (accept all jobs; no human in the loop)

- **Accept-all policy merged** (`become-dvm-interim` #7): `BECOME_MCP_JOB_ACCEPT=all`
  is the default. Anonymous callers may run `certify=true` and `bid_wei>0`
  jobs exactly as requested. No daily cap (owner). Only guards: per-client
  create rate limit and session-bound job ownership. Restricted mode
  `zero-bid` retains the previous gate. Extension point:
  `job_acceptance_decision()` in `http_api.py`.
- **Flags by PR + CI** (#5): non-secret flags in `config/host-flags.conf`;
  `mcp-suite` is the required check (~30 s).
- **Self-deploy** (PR open): box polls `origin/main` every minute via cron;
  one-time install on the box removes the last manual step.
- **Merging**: Claude merges its own PRs when `mcp-suite` is green (owner:
  no approval clicks). Grok Bot's merge-bot remains as a parallel path.
- **Hourly steward routine** created in the Claude session to sweep both
  repos for open PRs/issues/CI and act.

Risk accepted by owner, recorded here: the paid EC2 prove + Sepolia settle
path is now reachable by anyone with the URL, bounded only by the per-client
rate limit.

---

## Addendum 6: certified path verified anonymously; two restart bugs fixed

- **Certified ask with no key works end to end** (`become-dvm-interim` #7):
  `orch-45plus7-1e2c298d` and `orch-45plus8-d89ad7cd` (`certify=true`,
  `bid_wei=0`) were accepted, staged, uploaded to S3 and dispatched to the EC2
  prove workflow (runs 34500507641 / 34504608160) from an anonymous connector.
- **Re-certify guard bug** (#9): a free coqchk smoke of the same Target was
  mistaken for a Sepolia settle and blocked certify. Fixed; only certified
  priors count.
- **Ownership lost on restart** (#10): every anonymous create now returns
  `owner_token` and writes `anon_owner.json`; `become_status` /
  `become_result` / `become_cancel` accept the token from any session.
  Verified live: `orch-45plus8-d89ad7cd` readable with its token.
- **Settle watchers lost on restart** (#11): `also_settle=no` is by design
  (host settles after proof deposit), but the watcher was an in-process
  thread and self-deploy restarts the host on every merge. Host now re-arms
  watchers for dispatched, unsettled jobs on startup. Grok Bot's "may not
  produce a receipt" concern was correct for jobs spanning a deploy.
- Polling `become_status` early is harmless: the anonymous rate limit counts
  job creates only.

### Certified receipt confirmed (anonymous, zero bid, no key)

| | |
|---|---|
| Job | `orch-45plus8-d89ad7cd` (`exists n : nat, 45 + 8 = n`, `certify=true`, `bid_wei=0`) |
| Created | 16:51 UTC, anonymous connector; `owner_token` returned |
| Proof | EC2 run 34504608160 (Generate BECOME DVM Proof) |
| Settled | before 18:19 UTC, `agent_status=Settled`, `certified=true`, answer `53` |
| Fill tx | `0xf80dd2c8300717ad8d5f14679a58c9b2d44423a812902fc30a2c7bec67c3860f` |
| Receipt | https://sepolia.etherscan.io/tx/0xf80dd2c8300717ad8d5f14679a58c9b2d44423a812902fc30a2c7bec67c3860f |
| PayHook | `0x8FA889E7C6d9C74EA5ee2b4BFaf4DB8cE8e964B8` |
| Read via | `become_status` + `owner_token` from a fresh session (post-#10) |

End to end in ~90 minutes: URL in, no secret, EC2 proof, host auto-settle,
Sepolia receipt out. Grok Bot's `also_settle=no` concern is closed for jobs
created after #10/#11: the host settles and the receipt appears. Honesty
unchanged: T2CERT0 (guest certificate verify), Sepolia ≠ mainnet.


## Addendum 7: workflow audit of merged main, and data-model decisions (2026-09-10, later)

Full trace of the merged host (`become-dvm-interim` main `c244a7f`) is
summarised here; the data model it feeds is in
the internal schema document (private server repository).

**What the workflow is.** Python computes the sum and writes it into the
Coq answer file; Coq checks it. Job hash covers the question only. Smoke
"Settled" = coqchk passed on the host. Certified = tarball to S3, GitHub
Actions proof run on EC2, SP1 proof, then the host's own wallet opens and
fills the PayHook on Sepolia. Client, claimant and payer are the same wallet.

**Findings, ranked.**

1. **Jobs that never finish.** A failed or vanished Actions run leaves a job
   at `Proving`, `terminal:false` forever; the settle watcher gives up after
   3.5 h silently. Contradicts the documented "drive off terminal".
2. **Spend exposure under accept-all.** Anonymous `become_ask` accepts any
   `bid_wei`; the host wallet pays it to itself at fill. Rate-limit key falls
   back to the session id when no proxy header is present, and sessions are
   free to mint. Exposure is EC2 minutes and Sepolia test ETH. Owner rule is
   accept all, no cap; a server-side bid ceiling is a policy decision left
   to the owner.
3. **Restart window.** The 5-minute deploy tick restarts the host; a job
   restarted between claiming the dispatch slot and writing the ledger is
   left `pending` and never resumed.
4. **Blocking creates.** `certify:true` runs coqchk, an RPC lookup and
   tarball staging on the request thread even with `wait:false`.
5. **Leaks to anonymous callers.** Pre-Settled status includes the EC2
   staging recipe (S3 key, command lines); `become_result` returns host
   filesystem paths; `owner_token` is plaintext on disk; ownership is
   enforced only in the HTTP layer.
6. Session and rate-limit tables grow without bound; `become_cancel` sets a
   flag nothing reads.

**Owner decisions (data model).**

- Add `Deployment` as a first-class object: one live entry per chain,
  referenced by a job's `payhook`; source of truth is one file in the server
  repo, from which docs, the NIP-89 announcement and MCP server info derive.
- Client `Job` becomes a closed schema (36 fields, `additionalProperties:
  false`); operator data only under `diagnostics` when an operator key is
  presented; host paths and staging recipes never to any client.
- Drop `summary_for_model`, the duplicate `status`, and `visible`; clients
  drive off `terminal`, `how`, `agent_status`.
- Standardize requests on ERC-7683 (2026 resolver form); Nostr kinds 5700
  (request) / 6700 (result); registry text kept in the private repository.

**Next code pass (correctness, no policy content), in order.**

1. Stuck-job reaper: a failed or missing Actions run becomes `Failed` with
   `code` and `how`; watcher expiry also terminal.
2. Closed `Job` schema and `diagnostics` gating; strip host paths and
   `ec2_stage` from every client path (fixes finding 5 in one change).
3. `config/deployment.json` in the server repo, generated once from
   `toy-prod-pins.json`, read by `labels`, the encode tool and server info.
4. Move certify staging off the request thread.
5. Resume `pending` dispatch ledgers on restart; prune sessions and
   rate-limit tables.

Policy items left to the owner: server-side bid ceiling; rate-limit key
when no proxy header is present.
